const config = {
  theme: {
    extend: {
      fontFamily: {
        playfair: ['var(--font-playfair)', 'serif'],
      },
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;
